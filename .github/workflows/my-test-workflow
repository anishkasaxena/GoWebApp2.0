name: Test cleanup
on:
  workflow_dispatch:
jobs:
  fetch-packages:
    runs-on: ubuntu-latest
    steps:        
        - name: Fetch package
          run: |
            echo "::set-output name=packages::Anishka"
